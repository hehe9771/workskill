# 项目规则

## 文件输出约束

- 所有生成的 md 文件必须放到 `doc/` 目录下，禁止放在项目根目录或其他位置
- 所有临时文件必须放到 `doc/` 目录下，用完及时清理
- 任务过程中生成的代码文件统一存到 `code/` 目录下，按任务名新建子文件夹
  - 示例：`code/neo4j-etl/main_etl.py`、`code/smart-table/app.py`
  - 禁止将任务代码散放在项目根目录
